# improved-tribble

## Scaffold

1. create a pyhton virtual environment
2. create empty files : 
    `requirements.txt`, 
    `Dockerfile`, 
    `Makefile`, 
    `mylib/__init__.py`, 
    `mylib/logic.py`, 
    `main.py`
3. Populate `Makefile` (can put steps in the lifecycle of the project)- a tool that keeps you organized when building s/w proj,
